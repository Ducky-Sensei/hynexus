{
  "name": "hynexus-monorepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Full-stack monorepo with NestJS backend and React frontend",
  "scripts": {
    "prepare": "husky",
    "dev": "concurrently -n api,web -c blue,magenta \"pnpm --filter @hynexus/api dev\" \"pnpm --filter @hynexus/web dev\"",
    "dev:api": "pnpm --filter @hynexus/api start:dev",
    "dev:web": "pnpm --filter @hynexus/web dev",
    "build": "pnpm --filter @hynexus/types type-check && pnpm --filter @hynexus/api build && pnpm --filter @hynexus/web build",
    "build:api": "pnpm --filter @hynexus/api build",
    "build:web": "pnpm --filter @hynexus/web build",
    "test": "pnpm --filter @hynexus/api test && pnpm --filter @hynexus/web test",
    "test:api": "pnpm --filter @hynexus/api test",
    "test:web": "pnpm --filter @hynexus/web test",
    "test:e2e": "pnpm run test:e2e:setup && pnpm --filter @hynexus/web test:e2e",
    "test:e2e:setup": "docker compose up -d && pnpm run wait-for-services",
    "test:e2e:teardown": "docker compose down -v",
    "wait-for-services": "wait-on http://localhost:8001/health http://localhost:3001 -t 60000",
    "lint": "pnpm --filter @hynexus/api lint:check && pnpm --filter @hynexus/web lint",
    "lint:fix": "pnpm --filter @hynexus/api lint && pnpm --filter @hynexus/web lint:fix",
    "type-check": "pnpm --recursive type-check",
    "migration:generate": "pnpm --filter @hynexus/api migration:generate",
    "migration:create": "pnpm --filter @hynexus/api migration:create",
    "migration:run": "pnpm --filter @hynexus/api migration:run",
    "migration:revert": "pnpm --filter @hynexus/api migration:revert",
    "db:seed": "pnpm --filter @hynexus/api db:seed",
    "clean": "pnpm --recursive clean && rm -rf node_modules",
    "clean:install": "pnpm clean && pnpm install"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "wait-on": "^9.0.3"
  },
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
